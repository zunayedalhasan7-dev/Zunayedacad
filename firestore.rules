rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Connectivity test
    match /_health/{docId} {
      allow read: if true;
    }

    match /_health_test/{docId} {
      allow read, write: if true;
    }

    // User profiles
    match /users/{userId} {
      allow read, write: if true; 
    }

    // Courses
    match /courses/{courseId} {
      allow read, write: if true; 
      
      // Lessons
      match /lessons/{lessonId} {
        allow read, write: if true; 
      }
    }

    // Enrollments
    match /enrollments/{enrollmentId} {
      allow read, write: if true;
    }
  }
}
